export class Todo {
  constructor(completed, title, dueDate, priority, tag) {
    this.completed = completed;
    this.title = title;
    this.dueDate = dueDate;
    this.priority = priority;
    this.tag = tag;
  }
}
